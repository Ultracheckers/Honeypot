module.exports = {
    // No need for darkMode: 'class' since we're not toggling
    theme: {
      extend: {
        colors: {
          // Your custom color palette
          primary: '#282a36',    // Main background
          secondary: '#44475a',  // Lighter background for cards/inputs
          accent: '#bd93f9',     // Purple accent
          text: '#f8f8f2',       // Light text
          muted: '#6272a4',      // Subdued text
          success: '#50fa7b',    // Pastel green
          info: '#8be9fd',       // Pastel blue
          warning: '#ffb86c',    // Pastel orange
          danger: '#ff5555',     // Soft red
        }
      }
    },
    // ...rest of your configuration
  }